/** Fallback for the @modal slot whenever no intercepted route is active
 * (plain /work, or a direct/hard navigation straight to /work/[slug]). */
export default function Default() {
  return null;
}
