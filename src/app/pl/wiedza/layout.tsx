import "@/styles/insights/tokens.css";

/** Polish sibling of src/app/insights/layout.tsx — see its comment. */
export default function InsightsLayoutPl({ children }: { children: React.ReactNode }) {
	return <>{children}</>;
}
